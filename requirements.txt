# ==============================================================================
# requirements.txt - Platformă Pulsoximetrie
# ==============================================================================

# === CORE DEPENDENCIES (original) ===
pandas>=2.0.0
dash==2.17.0
plotly>=5.18.0
kaleido>=0.2.1
watchdog>=3.0.0
pdfplumber>=0.10.0
Pillow>=10.0.0

# === AUTHENTICATION & SECURITY (new) ===
# Flask-Login pentru session management
Flask-Login==0.6.3

# Argon2 pentru password hashing (mai sigur decât bcrypt)
argon2-cffi==23.1.0

# SQLAlchemy pentru PostgreSQL ORM
Flask-SQLAlchemy==3.1.1
psycopg2-binary==2.9.9

# Flask-Migrate pentru database migrations
Flask-Migrate==4.0.5
alembic==1.13.1

# === CLOUD STORAGE (Cloudflare R2 - S3-compatible) ===
# boto3 pentru integrare cu Cloudflare R2
boto3==1.34.144

# === EMAIL SERVICE ===
# Brevo (ex-Sendinblue) API pentru email-uri
sib-api-v3-sdk==7.6.0
requests>=2.31.0

# Jinja2 pentru template-uri email (deja în Dash, dar specificăm versiunea)
Jinja2>=3.1.2

# === UTILITIES ===
# itsdangerous pentru token-uri securizate (deja în Flask, dar specificăm)
itsdangerous>=2.1.2

# email-validator pentru validare email-uri
email-validator==2.1.0

# python-dotenv pentru variabile de mediu (.env)
python-dotenv==1.0.0

# === PRODUCTION SERVER ===
# Gunicorn pentru WSGI production server (Railway/Render)
gunicorn==21.2.0
dash-uploader==0.6.1